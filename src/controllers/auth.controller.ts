export const refresh = (request, response) => {
  response.send("auth/ refresh huselt irlee");
};

export const signIn = (request, response) => {
  response.send("auth/:signIn Get huselt irlee");
};

export const signUp = (request, response) => {
  response.send("auth/ signUp huselt irlee");
};

export const resetPasswordRrequest = (request, response) => {
  response.send("auth/resetPassword huselt irlee");
};

export const verifyResetPasswordRrequest = (request, response) => {
  response.send("auth/ verifyResetPasswordRrequest huselt irlee");
};
export const resetPassword = (request, response) => {
  response.send("auth/ resetPassword huselt irlee");
};
